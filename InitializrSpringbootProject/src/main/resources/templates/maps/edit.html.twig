{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="margin-right:100px;">Maps Edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
         <div>{{ form_label(edit_form.pdfimage) }}
        {{ form_errors(edit_form.pdfimage) }}
        {{ form_widget(edit_form.pdfimage) }}
        <a href="{{asset(map.pdfimage|replace({"web/":""}))}}" width="100" height="100" target="_blank" />Click here</a>
    </div><br>
     <div>{{ form_label(edit_form.logo) }}
        {{ form_errors(edit_form.logo) }}
        {{ form_widget(edit_form.logo) }}
        <img src="{{asset(map.logo|replace({"web/":""}))}}" width="100" height="100" />
    </div>
        <input type="submit" value="Update" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('maps_index') }}">Back to the list</a>
        </li>
       
    </ul>
{% endblock %}
